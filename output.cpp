#include <bits/stdc++.h>
int main() {
    std::vector<int> buffer;
    size_t ptr = 0;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
}
    ++ptr;
    ++ptr;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
}
    ++ptr;
    while (buffer[ptr]) {
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr] = getchar();
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    ++ptr;
}
    --ptr;
    --ptr;
    --ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    --ptr;
}
    ++ptr;
    ++ptr;
    ++ptr;
    ++ptr;
    ++ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    --ptr;
}
    ++ptr;
    while (buffer[ptr]) {
    --ptr;
    --ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    --ptr;
}
    ++ptr;
    ++ptr;
    ++ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    --ptr;
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    ++ptr;
}
    --ptr;
    --ptr;
    while (buffer[ptr]) {
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
}
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    ++ptr;
    ++ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    --ptr;
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    ++ptr;
}
    --ptr;
    --ptr;
    --ptr;
    --ptr;
    --ptr;
    --ptr;
    --ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    ++ptr;
}
    --ptr;
    while (buffer[ptr]) {
    ++ptr;
    ++ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    ++ptr;
    ++ptr;
}
    --ptr;
    --ptr;
    --ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    --ptr;
}
    ++ptr;
    ++ptr;
    while (buffer[ptr]) {
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    --ptr;
}
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    --ptr;
    --ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
    ++ptr;
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    --ptr;
    --ptr;
    --ptr;
}
    ++ptr;
    ++ptr;
    ++ptr;
    ++ptr;
    ++ptr;
    --ptr;
    while (buffer[ptr]) {
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    ++ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]++;
    ++ptr;
    while (buffer[ptr]) {
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    --ptr;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
    ++ptr;
}
    --ptr;
    --ptr;
}
    ++ptr;
    ++ptr;
    while (buffer[ptr]) {
    if (ptr >= buffer.size()) {
        buffer.resize(ptr + 1);
    }
    buffer[ptr]--;
}
    --ptr;
    putchar(buffer[ptr]);
    ++ptr;
    ++ptr;
}
    putchar('\n');
    return 0;
}
